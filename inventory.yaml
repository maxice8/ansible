servers:
  vars:
    ansible_user: root
    username: max
    group_services:
      - node-exporter
      - cadvisor
  hosts:
    mika:
      host_services:
        - docker-registry
        - keep-vm-alive
        - prometheus
        - grafana
    koga:
      host_services:
        - gitea
        - syncthing