servers:
  vars:
    ansible_user: root
    username: max
    user_shell: /bin/bash
    group_services:
      - watchtower
      - borgmatic
    # Change this to the name of the tailscale network
    tailnet_name: neko-great
    # Change this to tha name of the domain
    domain_name: maxice8.com
  hosts:
    mika:
      host_services:
        - caddy
        - keep-vm-alive
        - portainer
        - forgejo
        - whoami
        - syncthing
        - beszel
        # TODO: fix this
        #- pocket-id
      user_shell: /usr/bin/fish
