servers:
  vars:
    ansible_user: root
    username: max
  hosts:
    shu:
      services:
        - gitea
        - syncthing
        - node-exporter
    mika:
      services:
        - keep-vm-alive
        - syncthing
        - docker
        - node-exporter
        - prometheus
