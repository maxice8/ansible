servers:
  vars:
    ansible_user: root
    username: max
    user_shell: /bin/bash
    group_services:
      - node-exporter
      - cadvisor
      - watchtower
  hosts:
    mika:
      host_services:
        - keep-vm-alive
        - prometheus
        - grafana
      user_shell: /usr/bin/fish
    shu:
      host_services:
        - gitea
        - syncthing
