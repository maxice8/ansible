servers:
  vars:
    ansible_user: root
    username: max
    group_services:
      - node-exporter
      - cadvisor
  hosts:
    shu:
      host_services:
        - gitea
        - syncthing
    mika:
      host_services:
        - keep-vm-alive
        - prometheus
        - grafana