services:
  ts-vaultwarden:
    image: tailscale/tailscale:latest
    hostname: vault
    container_name: ts-vaultwarden
    environment:
      - TS_AUTHKEY={{ vaultwarden_tsclient_key }}?ephemeral=false
      - TS_EXTRA_ARGS=--advertise-tags=tag:container
      - TS_SERVE_CONFIG=/config/serve.json
      - TS_STATE_DIR=/var/lib/tailscale
      - TS_USERSPACE=false
    volumes:
      - tailscale-data-vaultwarden:/var/lib/tailscale
      - /srv/vaultwarden/config:/config
    devices:
      - /dev/net/tun
    cap_add:
      - net_admin
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "tailscale status"]
      interval: 1s
      timeout: 5s
      retries: 60
  vaultwarden:
    image: vaultwarden/server:latest
    container_name: vaultwarden
    restart: unless-stopped
    environment:
      - DOMAIN=https://vault.{{ tailnet_name }}.ts.net
      - EXPERIMENTAL_CLIENT_FEATURE_FLAGS=fido2-vault-credentials,ssh-key-vault-item,ssh-agent
      - SIGNUPS_ALLOWED=False
    volumes:
      - /srv/vaultwarden/vw-data/:/data/
    depends_on:
      ts-vaultwarden:
        condition: service_healthy
    network_mode: service:ts-vaultwarden

volumes:
  tailscale-data-vaultwarden:
