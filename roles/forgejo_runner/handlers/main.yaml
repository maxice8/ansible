---
- name: Reload systemd and Restart Runner
  ansible.builtin.systemd:
    daemon_reload: true
    name: runner.service
    state: restarted

- name: Restart Forgejo Runner
  ansible.builtin.systemd:
    daemon_reload: true
    name: runner.service
    state: restarted
