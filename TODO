- Find a way to install monitord (rust cargo package), send its data to metrics and set up alerts for it
- Improve the forgejo-runner role so:
  - It register offline instead of requiring manual intervention
	- It uses tailscale for communication
