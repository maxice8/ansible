apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - persistent-volume-claim.yaml
  - deployment.yaml
  - service.yaml
  - network-policy.yaml

configMapGenerator:
  - name: argus-config
    files:
      - config.yml
