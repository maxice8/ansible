apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - persistent-volume-claim.yaml
  - credentials.sops.yaml
  - deployment.yaml
  - service.yaml
  - network-policy.yaml

configMapGenerator:
  - name: argus-config
    files:
      - config.yml
