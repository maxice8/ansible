apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - credentials.sops.yaml
  - deployment.yaml
  - service.yaml
  - routes.yaml
  - network-policy.yaml

configMapGenerator:
  - name: pomerium-config
    files:
      - config.yaml
