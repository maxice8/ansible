apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: netbird-dashboard
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/maxice8/ansible.git
    targetRevision: master
    path: kubernetes/apps/netbird-dashboard
  destination:
    server: https://kubernetes.default.svc
    namespace: netbird-dashboard
  syncPolicy:
    automated:
      enabled: true
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
